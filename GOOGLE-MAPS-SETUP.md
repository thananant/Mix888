# 🗺️ เปลี่ยนแผนที่ลูกค้าเป็น Google Maps — วิธีขอ API key (ทำครั้งเดียว)

หน้า **🗺️ แผนที่ลูกค้า** ในหลังบ้านจะเปลี่ยนเป็น Google Maps ทันทีที่ใส่ key ไว้ที่
**ผู้ใช้ระบบ → ⚙️ ตั้งค่าระบบ → Google Maps API key → บันทึก key**
และปุ่ม "📍 หาพิกัดร้านที่ยังไม่มี" จะเปลี่ยนไปใช้ Google หาที่อยู่ (ที่อยู่ไทยแม่นกว่า OpenStreetMap มาก)

ถ้ายังไม่ใส่ key (หรือ key ใช้ไม่ได้) ระบบจะใช้แผนที่ OpenStreetMap แบบเดิมให้เองอัตโนมัติ ไม่มีอะไรพัง

---

## ขั้นตอนขอ key (ใช้เวลาประมาณ 10 นาที · ต้องมีบัญชี Google + บัตรเครดิต/เดบิต)

1. เข้า <https://console.cloud.google.com/> แล้วล็อกอินด้วยบัญชี Google ของร้าน
2. **สร้างโปรเจกต์ใหม่** — กดชื่อโปรเจกต์ด้านบนซ้าย → *New Project* → ตั้งชื่อ เช่น `MixFresh Map` → *Create*
3. **เปิด Billing (ผูกบัตร)** — เมนู ☰ → *Billing* → *Link a billing account* → กรอกบัตร
   > Google บังคับให้ผูกบัตร แต่มี**โควตาฟรีรายเดือน** (โหลดแผนที่ 10,000 ครั้ง + หาพิกัด 10,000 ครั้ง ต่อเดือน)
   > การใช้งานระดับร้านเรา (เปิดแผนที่วันละไม่กี่ครั้ง · หาพิกัดร้านใหม่เดือนละไม่กี่สิบร้าน) **อยู่ในโควตาฟรี**
   > ถ้ากังวล ตั้ง *Budget alert* ไว้ที่ 100 บาท จะได้อีเมลเตือนก่อนถูกคิดเงินจริง
4. **เปิดใช้ API 2 ตัว** — เมนู ☰ → *APIs & Services* → *Library* → ค้นหาแล้วกด *Enable* ทีละตัว:
   - **Maps JavaScript API** (ตัวแสดงแผนที่)
   - **Geocoding API** (ตัวแปลงที่อยู่ → พิกัด สำหรับปุ่มหาพิกัด)
5. **สร้าง key** — *APIs & Services* → *Credentials* → *+ Create credentials* → *API key*
   จะได้ข้อความยาว ๆ ขึ้นต้นด้วย `AIza…` → คัดลอกเก็บไว้
6. **ล็อก key ให้ใช้ได้เฉพาะเว็บเรา** (สำคัญ — กันคนอื่นเอา key ไปใช้แล้วมาคิดเงินเรา)
   กดที่ key ที่สร้าง → *Edit API key*
   - *Application restrictions* → เลือก **Websites** → *Add* → ใส่ `https://thananant.github.io/*`
   - *API restrictions* → **Restrict key** → ติ๊ก *Maps JavaScript API* และ *Geocoding API*
   - *Save*
7. **ใส่ key ในหลังบ้าน** — เปิดหลังบ้าน → *ผู้ใช้ระบบ* → *⚙️ ตั้งค่าระบบ* → วางในช่อง *Google Maps API key* → *บันทึก key*
8. เปิดหน้า **🗺️ แผนที่ลูกค้า** — ใต้คำอธิบายสีหมุดจะขึ้น `● Google Maps` แสดงว่าใช้งานได้แล้ว
   จากนั้นกด **🎯 ปรับพิกัดที่ยังคร่าว** หนึ่งครั้ง ให้ Google หาที่อยู่จริงของร้านที่เคยได้พิกัดแค่ระดับอำเภอ

---

## ถ้าขึ้นเตือน

| ข้อความ | สาเหตุ | แก้ |
|---|---|---|
| ⚠️ Google Maps key ใช้ไม่ได้ … ใช้ OpenStreetMap แทนไปก่อน | key พิมพ์ผิด / ยังไม่เปิด Billing / ยังไม่เปิด Maps JavaScript API / ล็อกเว็บผิด | เช็คข้อ 3, 4, 6 แล้วรีเฟรชหน้า |
| ⚠️ key ยังไม่เปิดใช้ Geocoding API | เปิดแค่ Maps JavaScript API | ทำข้อ 4 ตัวที่ 2 แล้วรีเฟรช |
| ● OpenStreetMap · โหลด Google Maps ไม่ขึ้น | เน็ต/ตัวบล็อกโฆษณาบล็อก maps.googleapis.com | ปิดตัวบล็อกในเบราว์เซอร์แล้วรีเฟรช |

key นี้เป็นแบบ "ใช้ในหน้าเว็บ" (มองเห็นได้อยู่แล้ว) — ความปลอดภัยอยู่ที่การล็อกเว็บในข้อ 6 ไม่ใช่การซ่อน key
